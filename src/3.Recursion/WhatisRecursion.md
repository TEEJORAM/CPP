## Recursion
- A Function calling itself  and there'll some base condition that terminates the recursion

**Example:**
Type func(param)
{
    if(<base_condition>)
    {
        -------
        func(param);
        -------
    }
}

**Recursion has two phase:**
- Calling 
- Returning
